'use strict';

angular.module('origApp.constants')
        .factory('adminTemplate', function() {
            // Public API here
            var adminTemlpate = {
                details : {}
            };
            return adminTemlpate;
        });
