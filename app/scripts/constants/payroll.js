'use strict';

angular.module('origApp.constants')
        .factory('payroll', function() {
            // Public API here
            var payroll = {
                details : {}
            };
            return payroll;
        });
