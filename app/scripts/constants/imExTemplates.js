'use strict';

angular.module('origApp.constants')
        .factory('imExTemplates', function() {
            // Public API here
            var imExTemplates = {
                details : {}
            };
            return imExTemplates;
        });
